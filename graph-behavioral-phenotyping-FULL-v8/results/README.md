# Final result provenance

`results_v8.json` contains the frozen headline values produced by
`notebooks/component2_consolidated_v8.ipynb`.

The notebook is the primary source of truth. This JSON is provided so README
content and downstream code can reference a single consistent result summary.
